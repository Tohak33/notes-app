# Журнал поддержки

| ID | Описание | Шаги воспроизведения | Решение | Версия |
|---|---|---|---|---|
| BUG-01 | Пустой заголовок ломает структуру (заметка создается без имени). | 1. Добавить 2. Заголовок: оставить пустым. | Добавлена проверка `if not title.strip(): raise ValueError` в `add_note` и соответствующий тест. | 1.0.1 |
| BUG-02 | Поиск чувствителен к регистру (не находит "Apple" по запросу "apple"). | 1. Создать заметку "Apple". 2. Найти "apple". | Изменен SQL запрос в `search` на использование `LOWER()` для обеих сторон выражения. | 1.0.2 |
| IMP-01 | Пользователи забывают, какие категории существуют. | Нет. Запрос на улучшение. | Добавлен метод `get_categories` в `NotesManager` и пункт меню 7 в `main.py`. | 1.1.0 |
| CHG-01 | Категории также должны быть нечувствительны к регистру при фильтрации. | 1. Категория "Work". 2. Поиск по "work" ничего не выдает. | Обновлен метод `get_by_category`, добавлено приведение к `LOWER()`. | 1.1.1 |
| Q-01 | Как сделать резервную копию заметок? | - | Ответ пользователю в Issue: "Достаточно скопировать файл notes.db в безопасное место". Изменения кода не требуются. | - |
